node_modules/
npm-debug.log
.DS_Store
.env
.env.local
.vscode/
.idea/

# Per-user runtime state (overrides + audit log). Regenerate by using the app.
data/state.json
data/decisions.jsonl

# Per-env snapshots from the in-app prompt editor.
prompts/history/
